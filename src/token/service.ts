

export const tokenService = async (id: number) => {

    // verify token exists in nft collection

    // check if nft image is already cached

    // if not, generate nft image and cache it

    // return nft json data

}
